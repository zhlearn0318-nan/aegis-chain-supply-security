# P0-3 当前状态真值验收摘要

本轮没有修改检测器或策略，而是解决“不同报告都自称当前”的工程风险。仓库根现在只有一个 `CURRENT_STATUS.md` 承担当前能力、数字、发布判断和下一项；M1–M4、7 月复现和评委审查继续作为不可覆盖的历史证据。

6 项文档契约、341 项后端回归、10 项前端测试和生产构建全部通过。当前入口不再出现 329/335 的旧后端总数，P0-2 动态队列 API 字段和失败语义已同步。

结论为 `single_current_status_truth_enforced`。下一工程节点为 P0-4：项目自身依赖、许可证、NOTICE、SBOM 和自扫描。
