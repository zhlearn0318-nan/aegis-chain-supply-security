# PLAN：Skill 动态审计 20 样本稳定性回归 v2

## 1. 修正原因

- run id：`2026-08-27-skill-dynamic-regression20-v2`
- 实验层级：`auxiliary/dev-regression`
- v1 按原预期 60/60 通过，但运行后审查发现 `os.system()` 的风险标签过宽松。
- v2 将任何 `os.system()` 调用视为系统 Shell 执行，规则为 `AEGIS_DYNAMIC_SHELL_SPAWN`、严重度 `CRITICAL`、决策 `BLOCK`。

## 2. 假设与接受线

- H0：修正规则后，60 次执行至少出现一次决策/规则不匹配、跨轮不稳定、遥测缺失、清理失败或容器残留。
- H1：修正后的 20 个样本连续 3 轮全部符合安全语义，负向指标全部为 0。
- 接受线：20 个样本、8 类行为、3 轮、60 次执行；决策正确与必需规则召回均为 60/60；良性误报、危险漏报、复核错配、跨轮不稳定、遥测缺失、清理失败、容器残留均为 0。

## 3. 固定条件

样本代码、文件哈希、容器隔离合同、固定镜像、随机种子 `20260827` 与 v1 相同。唯一策略变化是 `os.system()` 从普通子进程复核提升为 Shell 执行阻断。仍不使用 GPU、云服务、互联网或第三方样本。

## 4. 结论边界

v2 仍是自建受控样本回归，不代表第三方真实 Skill 的泛化检测率；Python audit hook 不是不可绕过的内核安全边界，Docker Desktop/WSL2 也不等同于专用恶意代码分析虚拟机。
