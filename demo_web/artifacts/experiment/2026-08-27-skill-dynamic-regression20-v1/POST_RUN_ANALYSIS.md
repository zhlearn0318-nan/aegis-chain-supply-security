# v1 运行后审查

v1 按预注册标准完成 20 个样本 × 3 轮，60 次执行均与当时预期一致。但运行后进行工程语义复核时发现：`process_os_system` 被预期为普通子进程 `REVIEW`，而 Python `os.system()` 实际通过系统 Shell 执行命令，应按 Shell 启动直接 `BLOCK`。

因此，v1 的“60/60”只能证明实现与原预期一致，不能证明原预期完全正确。v1 原始结果不覆盖、不改写；修正规则和标签后建立 v2，并重新执行全部 60 次回归。这个缺口不是运行随机失败，而是安全策略语义偏宽松。
