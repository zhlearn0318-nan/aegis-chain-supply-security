# M24 E02 执行修订 v3

v2 已修正分析器白名单，但实验封装错误地对 `backend.policy.summarize()` 返回的普通字典再次调用 `model_dump()`，导致 39 条成功流水线被封装层转成 UNKNOWN；另 1 条为独立运行时失败。v2 标签未打开、输入未变化，结果无效并保留失败凭据。

v3 仅删除这次多余的 `model_dump()`。冻结语料、Cisco/Aegis 生产流水线、策略、并发、超时、指标和失败闭锁原则均不变。
