# M23 E02 执行修订 v2

E02 冻结语料、来源提交、人工分层与接受门保持不变。首次 F0 执行因实验运行器的分析器名称白名单写错，将 39 个已经完成的静态流水线误判为越界；另 1 个案例为运行时失败。40 条均失败闭锁为 UNKNOWN，标签未打开、输入未变化，但该运行不得作为实验结果。

v2 只修正白名单名称：

- `bytecode_analyzer` 改为生产流水线实际报告的 `bytecode`；
- `pipeline_analyzer` 改为 `pipeline`；
- `aegis-custom-rule-v1` 改为 `aegis-custom-rules-v1`。

其余扫描器、策略、样本、并发、超时和指标合同不变。v1 失败记录保留，v2 写入新目录，不覆盖旧输出。
