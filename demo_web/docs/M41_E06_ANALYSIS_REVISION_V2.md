# M41 E06 分析方法修订 v2

v1 结构门通过，但人工抽查发现两项诊断方法问题：防御语境判断使用全文模式，可能被远离 Finding 的词语干扰；代表案例允许同一 case 承担两个角色，且正确检出沿用了错误样本的建议字段。

v2 保持父锁箱、60 个漏报和全部 54 个误报/UNKNOWN 样本完全不变，只做两项修正：

1. 语义误报使用 Finding 所在行前后各 3 行的局部窗口归因；没有行号时才退回全文；
2. 8 个代表案例必须 case_id 唯一，正确检出的建议处置保持原决策，不再显示为 ALLOW。

v1 目录保留，不承担最终 E06 结论。v2 仍为固定量表的单标注者诊断，不报告 Cohen's kappa，也不据此修改同一锁箱规则。
