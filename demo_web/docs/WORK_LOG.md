# 开发台账

## 2026-08-07：接管现有资产并建立最小可信闭环

### 第 1 步：环境盘点

做了什么：检查工作区、Python、Cisco CLI、已有报告和运行环境。

结果：全局 Python 为 3.12.4，全局 PATH 中没有两个 Cisco 命令，但 `supply_chain_reproduction` 已保存独立 Python 3.11/3.13 运行环境、固定 wheel、官方源码和评测结果。根目录的 `.git` 是空目录，不是有效仓库。

为什么：避免重复安装，也避免把“全局找不到命令”误判成“扫描器不可用”。

### 第 2 步：读懂旧报告并固定对照基线

做了什么：读取 `availability_summary.json`、固定提交版本和现有小样本指标。

结果：Skill 小样本集在 HIGH/CRITICAL 阈值下 Recall 为 0.2857、F1 为 0.4；MCP 的 6 个静态对象全部分类正确，但规模太小。两者适合作为底座，不足以直接作为最终检测方案。

为什么：后续所有自研能力必须和同一基线比较，否则无法证明改进来自哪里。

### 第 3 步：真实 API 冒烟测试

做了什么：启动 FastAPI 网关，调用 `skill-risky` 预置样本，并轮询统一结果。

结果：任务 `153e4820691a4cf2996902920cef3dad` 在 4901 ms 内完成，输出 4 条 Finding，其中 `DATA_EXFIL_HTTP_POST` 为 CRITICAL，最终决策为 `BLOCK`。证据包含文件 `scripts/format.py`、第 13 行、代码片段、规则 ID 和制品 SHA-256。

为什么：证明“上传/选择样本 → Cisco 扫描 → 归一化 → 门禁决策 → 存储”这条链路是真实的，不是 UI 假数据。

### 第 4 步：补自动测试

做了什么：增加门禁决策、Skill 结果归一化、空报告失败闭锁、安全解压和 MCP JSON 拆分测试，并提供 `run_tests.ps1`。

结果：11 项测试全部通过。测试环境的临时文件固定写入项目 `data/test-temp`，避免系统临时目录权限造成假失败。

为什么：后续拆分 700 行单文件时，可以确认重构没有悄悄改变安全结论。

### 第 5 步：修正 Windows 启动兼容性

做了什么：启动脚本改为 ASCII，规避 Windows PowerShell 5 对 UTF-8 无 BOM 中文脚本的错误解码；同时在子进程启动前归一化 `Path/PATH`，并把 stdout/stderr 写入日志文件。

结果：服务重新启动成功，`/api/health` 返回 `ready`，Skill Scanner 2.0.13、MCP Scanner 4.8.2 和依赖审计均为可用状态。

为什么：启动脚本必须在中文 Windows 和 Codex 终端中都可复现，不能依赖某一次会话的环境变量。

## 已知非阻塞问题

- FastAPI 的 `@app.on_event("startup")` 出现弃用警告；在 M1 拆分后端时改为 lifespan。
- 当前准确率来自很小的 smoke test；最终材料不得把 MCP 6/6 写成普遍 100% 准确率。
- 动态验证尚未实现；在隔离环境准备好之前，不执行恶意样本脚本。

## 下一次开发任务

将 `backend/app.py` 拆分为可维护模块，并先固定统一 Finding 的 Pydantic 数据模型。拆分前后 API 行为必须由本次测试保护，不能边重构边改变检测结论。

## 2026-08-10：M1.3 YAML 准入策略与策略证据

### 第 1 步：锁定等价迁移契约

做了什么：以 M1.2 的 `27 passed` 和三类真实扫描结果为只读基线，明确默认阈值、样本、扫描器和 Finding 数量均不得变化。

结果：确定只把现有严重度门禁迁移到 YAML，并将新结果契约从 `1.0` 小版本升级到 `1.1`。

为什么：本次目标是提高可配置性和可审计性，不把策略迁移伪装成检测性能提升。

### 第 2 步：实现严格 YAML 策略

做了什么：新增 `config/admission_policy.yaml` 与 Pydantic 策略模型，配置策略 ID、版本、阻断/复核/允许严重度和 `fail_closed`。

结果：系统会拒绝严重度集合重叠、覆盖不完整、把 `UNKNOWN` 放入普通集合，以及 `fail_closed: false` 的配置。

为什么：无效或含糊配置不能静默加载，更不能产生错误放行。

### 第 3 步：增加 policy_trace

做了什么：新增策略 ID、版本、命中规则、判定原因、命中严重度、Finding ID 和失败闭锁状态；成功扫描统一经过一个完成函数，超时、配置错误和扫描器异常使用不同失败规则。

结果：新任务使用契约 `1.1`；旧 `1.0` SQLite 记录仍能读取，缺失的策略信息明确显示为 `unresolved`，不会假装由新策略评估过。

为什么：答辩和平台审计都需要说明“哪个版本的什么规则依据哪些证据做出决定”。

### 第 4 步：修复依赖 Finding 重复 ID

做了什么：真实回归发现 `pip-audit` 上游结果存在重复漏洞记录，导致两个 Finding ID 重复；归一化时保留首个 ID，并为后续重复项追加出现序号。

结果：依赖样例仍保持 14 条 HIGH Finding 和 `BLOCK`，但 Finding ID 与策略引用都达到 14/14 唯一。

为什么：风险数量和基线决策不能因本次工程重构改变，但审计标识必须能唯一定位。

### 第 5 步：测试、真实回归与证据固化

做了什么：增加策略加载、等价决策、配置拒绝、旧记录兼容、失败闭锁、重复 ID 和 Markdown 导出测试，并真实运行 Skill、MCP、依赖三个预置对象。

结果：`45 passed, 0 warnings`。Skill 为 `BLOCK/4/1 CRITICAL`，MCP 为 `BLOCK/7 HIGH`，依赖为 `BLOCK/14 HIGH`；制品哈希和 M1.2 相同。证据保存于 `artifacts/experiment/2026-08-10-m1-3-policy-config/`。

为什么：代码、测试、策略快照、真实任务 ID 和源文件哈希共同组成可复现证据，避免仅凭页面截图汇报。

## 当前下一任务

以 schema `1.1` 为基础定义 `/api/v1` 的请求、响应与统一错误码；先保持旧 `/api` 可用，再通过契约测试冻结新接口。

## 2026-08-10：M1.3 `/api/v1` 对接契约

### 第 1 步：冻结兼容边界

做了什么：以 45 项测试、schema `1.1` 和策略 `1.0.0` 为基线，确定 v1 只增加接口 envelope 和错误码，不改变扫描器、策略、Finding 或 SQLite。

结果：新 `/api/v1` 与旧 `/api` 共享同一套扫描函数；旧接口继续返回原始数据与 `{detail}` 错误，新接口返回 `{api_version, data}` 或 `{api_version, error}`。

为什么：统一平台需要稳定契约，但现有网页和脚本不能因升级立即失效。

### 第 2 步：实现类型化 v1 与机器错误码

做了什么：新增 `backend/api_contract.py` 和 `backend/api_v1.py`，实现 9 个 v1 路径、HTTP 202 异步创建、13 个稳定错误码及 400/404/405/413/422/500 处理。

结果：OpenAPI 共有 18 个业务路径，旧版与 v1 各 9 个；没有重复 operation ID。未知 API 路径不会再落入 React 兜底并返回 HTML 200。

为什么：调用方必须通过状态码和错误码可靠处理失败，而不是解析中文文本或误收网页内容。

### 第 3 步：保持后端分层

做了什么：先完成内联原型，再把 v1 路由与异常处理提取到 `backend/api_v1.py`。

结果：`backend/app.py` 从内联试验的 864 行下降到 678 行；v1 层只注入旧操作函数，没有复制扫描编排。

为什么：接口版本增加不应让核心应用重新退化为超大单文件。

### 第 4 步：测试与真实双路回归

做了什么：增加 envelope、202、已知错误、422、500、404/405、OpenAPI、导出和旧接口兼容测试；随后运行旧 Skill、v1 Skill、v1 MCP 和 v1 依赖。

结果：`58 passed, 0 warnings`。旧 Skill 与 v1 Skill 的制品哈希、`BLOCK`、4 条 Finding、1 条 CRITICAL 和策略规则完全一致；v1 MCP 为 7 HIGH，v1 依赖为 14 HIGH。未知 v1 路径真实返回 `404 / API_ROUTE_NOT_FOUND`。

为什么：既证明新契约可以使用，也证明 HTTP 包装层没有改变安全结论。

## 当前下一任务（更新）

先把 `policy_trace` 和 v1 错误状态展示到前端，再开始 SkillTrustBench 官方来源、许可证和字段审计，以及 50–100 条样本适配。

## 2026-08-10：M1.3 前端 v1 对接与策略证据展示

### 第 1 步：审计现有前端基线

做了什么：盘点 React 单页中的接口调用、任务状态、错误处理、策略字段和构建产物，并先执行一次生产构建。

结果：基线构建通过，但所有调用仍使用旧 `/api`，错误只读取 `{detail}`，没有显示 `policy_trace`，也没有前端测试。基线为 15 个模块、JavaScript 207.83 kB、CSS 14.41 kB。

为什么：先明确“接口迁移”与“视觉调整”的边界，避免在接入过程中修改扫描器或安全结论。

### 第 2 步：建立单一 v1 客户端

做了什么：新增 `frontend/src/api.js`，集中管理 `/api/v1`、成功 envelope、HTTP 202、错误 envelope、网络失败、畸形响应闭锁、上传和导出 URL。

结果：页面组件不再自行拼接接口；后端的 `status/code/message/details` 会完整保留。成功响应若不是 `{api_version: "v1", data: ...}`，客户端返回 `INVALID_API_ENVELOPE`，不会把 HTML 或旧响应误当正常数据。

为什么：统一平台接入需要稳定的机器契约，不能依靠中文错误文本或散落在组件里的 URL。

### 第 3 步：补齐异步状态与策略证据

做了什么：将页面全部切换到 v1，明确展示 `HTTP 202 / ACCEPTED`、queued、running 和终态；把轮询改为非重叠退避；增加策略健康卡以及策略 ID、版本、命中规则、原因、严重度、Finding 引用和失败闭锁展示。

结果：操作者能够区分“任务已接收但未完成”和最终 `UNKNOWN`，也能回答“哪个策略依据哪条证据作出阻断”。全局错误和上传错误同时显示稳定错误码与可读信息。

为什么：比赛展示不能只给一个风险颜色；政企准入还需要审计依据和失败处置语义。

### 第 4 步：测试、构建与真实回归

做了什么：使用 Node 内置测试器增加 7 项客户端测试；执行 Vite 生产构建、58 项后端回归和真实 `skill-risky` v1 扫描；核验 JSON/Markdown 导出。

结果：前端 `7 passed`，后端 `58 passed`；生产构建为 16 个模块、JavaScript 213.37 kB、CSS 17.33 kB。真实任务 `455f1f4411ae49359231517b0700450d` 创建返回 `202/queued/PENDING_SCAN`，终态为 `completed/BLOCK`，4 条 Finding、1 条 CRITICAL，命中 `POLICY_BLOCK_SEVERITY`；两种导出均为 200。

为什么：单元测试证明客户端契约，真实扫描证明接口包装和 UI 所依赖字段没有改变 Cisco 结果。

### 第 5 步：记录环境异常而不误操作

做了什么：发现本机 8000 端口已有一个无法确认归属的旧 Python 进程，其 v1 请求返回 HTML；没有直接结束该进程，改用项目应用和真实适配器完成隔离回归。

结果：新客户端会将这类错误识别为 `INVALID_API_ENVELOPE`。同时修复启动脚本的空 PID 文件误判：现在以 `/api/v1/health` 为准，端口被不兼容服务占用时明确报错，且不会误停进程。证据已保存到 `artifacts/experiment/2026-08-10-m1-3-frontend-v1/`。

为什么：安全工具自身也必须避免误停宿主机上的未知进程，同时需要把环境问题与产品代码失败分开记录。

## 当前下一任务（2026-08-10）

开始 M2 数据集入口审计：核验 SkillTrustBench 的论文/官方仓库、许可证、下载方式、样本字段、标签和潜在执行风险，并形成 50–100 条试运行 manifest；在数据来源和标签冻结前不新增规则。

## 2026-08-10：M2 SkillTrustBench 数据入口与 90 条 pilot 基线

### 第 1 步：核验一手来源和许可

做了什么：核验腾讯朱雀项目页、官方发布文章、Hugging Face 数据仓库、审计刷新提交和官方结果仓库；检查字段、T01–T09 分类、安全警告与 `CC BY-NC-SA 4.0`。

结果：确认该数据集由腾讯朱雀实验室与香港中文大学（深圳）联合发布，适合非商业竞赛/研究静态评测。没有把它表述为同行评审国际标准；本次检索未发现专门的数据集论文。

为什么：公开可下载不等于可以随意使用，比赛材料必须同时说明发布方、版本、许可和证据边界。

### 第 2 步：解决上游版本与校验冲突

做了什么：对比项目页、当前数据卡、审计提交和 Hugging Face 官方 API。

结果：确认 audited refresh 将 310 条 `suspicious` 升级为 `malicious`，正确分布为 `2863/1014/1643`；同时发现 README 的 SHA-256 表仍指向刷新前对象。导入器因此固定 revision `762d538…`，普通文件按 Git blob SHA-1 与字节数核验，ZIP 按 LFS SHA-256 与字节数核验，并额外记录本地 SHA-256。

为什么：直接照抄 README 旧哈希会把官方审计后的正确数据误判为损坏，也会造成标签口径混用。

### 第 3 步：实现失败闭锁的安全导入器

做了什么：新增 `tools/datasets/prepare_skilltrustbench.py`，限制 ZIP 成员数、总大小和单文件大小，拒绝绝对路径、目录穿越、Windows 非法路径和符号链接；不导入、不安装、不执行样本。

结果：下载并核验 4 个固定对象；全量 ZIP 含 37,721 个成员、5,520 个 case。只解压 pilot 所需 case，并将压缩包和解压文件设为只读。

为什么：即使标签为 `normal` 也不能把未知 Skill 当作宿主机安全文件，静态评测入口本身必须具备供应链防护。

### 第 4 步：冻结 90 条 pilot

做了什么：使用 `SHA-256(seed + ':' + case_id)` 确定顺序，先在非正常样本中贪心覆盖 T01–T09，再填满三类各 30 条。

结果：pilot 共 90 条、628 个文件、2,774,819 字节；T01–T09 计数为 `16/2/9/41/22/2/1/4/29`。case ID 清单 SHA-256 为 `59dd01…8800`，manifest SHA-256 为 `0302a9…806f`。

为什么：平衡 pilot 便于快速发现适配与分类问题，确定性清单可防止看到扫描结果后替换不利样本。

### 第 5 步：冻结指标契约

做了什么：固定 `ALLOW→normal`、`REVIEW→suspicious`、`BLOCK→malicious`，将 `UNKNOWN/failed` 作为 abstain；以 strict macro F1 为主指标，同时强制报告 coverage、failure rate、恶意召回/FNR、非正常召回、正常 FPR、T01–T09 召回和耗时。

结果：契约保存到 `baseline/skilltrustbench_v1_0/json/metric_contract.json`。运营门禁的“不自动放行率”与检测准确率严格分开。

为什么：指标必须在首轮扫描前冻结，否则容易通过改映射或忽略失败样本得到虚高数字。

### 第 6 步：修复可重复性细节并验证

做了什么：把清单路径改为相对路径；增加已有 pilot 的 ID、目录和 case tree hash 复核；下载先验证 `.part` 再原子替换；固定生成文件为 LF 换行；在工作区根 `.gitignore` 加入原始数据排除规则。当前工作区没有有效 Git 仓库，因此没有擅自初始化，也不能声称已通过 `git check-ignore` 验证。

结果：同一导入命令第二次运行成功，没有重新下载或重新解压；全部源对象和 90 个 case tree hash 一致。后端测试达到 `63 passed`。

为什么：真正的可复现不只是“第一次能跑”，还要能在缓存、换机和重复执行时发现任何数据漂移。

## 当前下一任务（M2）

实现批量静态评测运行器：先选择固定 pilot 中 3–5 条做输入适配和超时冒烟，再运行全部 90 条并输出逐样本 JSONL、指标、混淆矩阵及 FP/FN 清单。首轮基线结果保存前不新增检测规则。

## 2026-08-10：M2 Cisco Skill Scanner × SkillTrustBench pilot90

### 第 1 步：实现安全批量运行器

做了什么：新增 `tools/evaluation/run_skilltrustbench.py`，在扫描前验证 revision、pilot manifest 和 case tree hash，顺序调用 Cisco，复用现有归一化和 YAML 策略，并输出逐样本结果与指标。

结果：结果文件不保存 Cisco 原始报告或 evidence/snippet；扫描子进程会移除外部分析器 API key。新增选择、abstain 指标、脱敏和错误切片测试，总测试达到 `67 passed`。

为什么：正式跑分不能靠手工逐条复制，也不能因结果持久化而把恶意样本正文传播到源码证据目录。

### 第 2 步：固定 5 条 smoke

做了什么：在运行前固定 2 normal、1 suspicious、2 malicious，共 5 条；使用默认本地静态命令扫描。

结果：5/5 完成、failure rate 0，全部使用 `static_analyzer/bytecode/pipeline`，样本 hash 前后一致。smoke strict macro F1 为 0.4889，但只作接线诊断。

为什么：先用最小运行暴露命令、输出和安全问题，避免直接在 90 条上发现基础错误。

### 第 3 步：运行固定 90 条主基线

做了什么：按冻结 manifest 顺序运行全部 90 条，运行中不修改规则、策略、映射或样本。

结果：耗时 362.3 秒；90/90 有终态，89 completed、1 abstain。coverage 98.89%，strict macro F1 0.5114，malicious recall 80%，malicious FNR 20%，non-normal recall 78.33%，normal FPR 33.33%。

为什么：这些数字第一次把“Cisco 工具能运行”推进到“在公开固定数据上的能力和短板可测量”。

### 第 4 步：核对混淆矩阵和错误清单

做了什么：独立核对逐样本结果、混淆矩阵、10 个 FP、6 个 malicious FN 和 41 个总分类错误；检查 T01–T09 support 与 pilot 统计一致。

结果：normal 为 `20/7/3/0`，suspicious 为 `9/5/15/1`，malicious 为 `3/3/24/0`。T09 召回最低，为 65.52%；T02/T06/T07 虽为 100%，但 support 只有 2/2/1。

为什么：只给总体 accuracy 会掩盖 normal 误报和 suspicious 边界问题，也会误用小 support 的 100%。

### 第 5 步：诊断唯一失败并接受基线

做了什么：不读取或保存原始错误文本，只比较安全错误标记、消息哈希和 parser 结构。

结果：`case_02187` 的 UTF-8 和 PyYAML frontmatter 正常，但 Cisco 的 `python-frontmatter` 严格路径返回 `ScannerError`。主结果保留 `UNKNOWN/abstain`，没有用 `--lenient` 覆盖。基线以 `accepted_with_caveats` 接受。

为什么：失败率本身是评测结果；为了让数字更漂亮而切换宽松模式会破坏可比性。

## 当前下一任务（错误分析）

按 10 个 normal 误报、6 个 malicious 漏判、15 个 suspicious→malicious 和 T09 漏检建立错误家族，选择 2–3 个自研证据关联/规则方向，再在同一 90 条上做配对对比。

## 2026-08-14：SkillTrustBench 官方 556 条扩大样本复核

### 第 1 步：锁定官方固定子集

做了什么：锁定 `SkillTrustBench-results` 提交 `326ec286…`，下载固定 10% 清单、评测协议和榜单记录；复算排序 ID 的 SHA-256，并与官方榜单逐项核对。

结果：确认 556 条唯一 ID，标签分布为 normal 166、suspicious 105、malicious 285；子集文件 SHA-256 为 `dff762…6843`，ID 哈希为 `903a03…6c2`。556 条与完整 ground truth 及 ZIP case 目录全部一致。

为什么：扩大样本不能靠随机追加；使用官方固定子集才能让汇报对象复核同一份输入。

### 第 2 步：实现可重复、安全的官方子集导入

做了什么：新增 `prepare_skilltrustbench_official_subset.py`，固定官方文件 URL、revision、大小和 SHA-256；复用 ZIP 安全检查，只解压清单中的 case，生成逐 case tree hash 和只读文件。

结果：556 条完成导入，其中 553 条可读，3 条真实恶意样本被 Windows Defender 阻止读取。后续重复导入会复核归档身份，不重新释放已隔离样本。

为什么：端点防护命中本身是安全事件，不能为追求 100% 扫描覆盖而关闭 Defender 或添加排除目录。

### 第 3 步：扩展评测器

做了什么：增加 `official10` 模式、固定输入预检、统一策略层 loose non-normal 指标和 `--resume`；恢复时要求逐 case 结果是冻结清单的精确前缀，并复核数据、扫描器、策略、指标契约和样本 hash。

结果：新增指标和恢复测试通过；完整后端测试最终达到 `73 passed`。

为什么：556 条需约 40 分钟，断点能力可以避免环境中断后从头重扫，同时防止把不同契约的两次结果拼接到一起。

### 第 4 步：运行 556 条并安全恢复

做了什么：使用 Cisco Skill Scanner `2.0.13.dev3+g4dee90371` 顺序扫描；所有子进程强制使用本地 LiteLLM 价格表与 Hugging Face/Transformers 离线模式，并移除云服务凭据。

结果：首次完成 529 条后，Defender 已隔离的 `case_05527/SKILL.md` 触发预检停止。修正“端点隔离样本只使用固定归档身份”的终态逻辑后，复核 529 条前缀并从断点完成剩余 27 条。最终 556/556 有终态，546 completed、7 RuntimeError、3 EndpointProtectionBlocked；总墙钟约 39 分钟。

为什么：批次停止暴露了真实宿主机安全边界；正确处理是把隔离状态纳入评测，而不是绕过端点防护。

### 第 5 步：独立验收与结果分析

做了什么：新增 `verify_skilltrustbench_run.py`，核验 8 个输出的大小/SHA-256，从逐 case JSONL 重算指标、混淆矩阵和错误切片，并检查分析器集合、Finding 脱敏和 before/after hash。

结果：全部验收通过。coverage 98.20%、strict macro F1 0.4977、malicious recall 72.28%、normal FPR 24.70%；策略层 precision 87.94%、recall 76.67%、loose F1 81.92%。553 条可读样本 hash 无变化。

为什么：扫描结束不等于结果可信；必须证明汇总数字能由逐案结果复算，且运行没有修改样本或泄露原始证据。

### 第 6 步：形成下一轮任务

做了什么：分析 41 条 normal 误报、91 条风险筛查漏报、79 条 malicious 严格漏判和 T01–T09 切片，并与 90 条 pilot 作有限对照。

结果：wild real-world 风险召回仅 22.22%，crypto_wallet 为 59.26%；T06/T09/T05 召回为 40.00%/62.50%/66.67%。下一轮先做这些漏报的最小证据分析，再处理网络/文件访问规则的上下文误报。

为什么：官方 556 条表明 90 条 pilot 的恶意严格召回偏乐观；后续增量应由扩大样本暴露的稳定短板驱动，而不是继续堆叠厂商工具。

## 当前下一任务（M3 前置错误分析）

从 91 条 risk-screening miss 中先选择 wild real-world、crypto_wallet、T06/T09/T05 的 20–30 条代表样本，只提取脱敏静态特征和缺失规则，不修改最终 556 条标签或样本；形成 2–3 个候选证据关联规则后再冻结开发/复测边界。

## 2026-08-14 至 2026-08-15：SkillTrustBench 全量 5,520 条扫描

### 第 1 步：增加全量安全导入

做了什么：新增 `prepare_skilltrustbench_full.py`，固定完整 ground truth、80 MB ZIP 和排序 ID 的 SHA-256；全量解压时逐 case 建立 tree hash、设置只读并持续写入断点清单。

结果：5,520 条标签与冻结真值一致；5,451 条可扫描，61 条被端点防护阻止读取，8 条包含 Windows 不允许的路径字符。平台不兼容案例不改名、不做不完整扫描，只保留归档身份并记为 scanner-ineligible。

为什么：改名会改变样本，关闭安全软件会扩大宿主机风险；UNKNOWN 比伪造完整扫描更可信。

### 第 2 步：实现有界并发

做了什么：给评测器增加 `full` 模式和 `--workers`。并发按固定顺序分批，每批最多 N 条；结果仍按冻结 ID 顺序逐条落盘，日志加锁，恢复时冻结 worker 数、扫描器、策略和数据身份。

结果：5 条顺序/四路并发对照的最终决策、分析器、告警汇总、策略轨迹与规则集合完全一致。四路配置通过后用于全量，单条前后 hash 校验和安全停止规则保持不变。

为什么：Cisco CLI 没有原生多样本 `--jobs` 参数，外层有界并发可以降低重复冷启动带来的总墙钟时间，又不让 5,520 个任务一次性无界提交。

### 第 3 步：完成全量运行

做了什么：使用 Cisco Skill Scanner `2.0.13.dev3+g4dee90371`、本地三个静态分析器、150 秒单条超时和四路并发扫描完整数据。

结果：5,520/5,520 产生终态，5,372 completed、148 abstain；abstain 为 61 EndpointProtectionBlocked、8 PlatformPathIncompatible、79 RuntimeError。活跃墙钟 8,725.938 秒（145.43 分钟），吞吐 37.96 条/分钟；无 fatal error 和样本 hash 变化。

为什么：全量结果能检验 556 条官方子集是否存在抽样偏差，并给动态审计和语义增强提供更稳定的错误边界。

### 第 4 步：独立验收和重叠复现

做了什么：扩展 `verify_skilltrustbench_run.py` 支持 full 与并发执行指标，重新核验所有输出身份并从逐案结果复算；同时将完整结果与官方 556 条清单按 ID 对齐。

结果：指标、混淆矩阵、错误切片、输出 SHA-256、分析器白名单、Finding 脱敏与 tree hash 全部通过。556 条重叠中 546 条两轮均完成 Cisco 扫描；忽略根目录绝对路径后，决策与规则集合 0 差异，终态 0 差异。后端测试 `73 passed`。

为什么：并发可能引入共享缓存、日志竞争或非确定性，重叠复现为“加速没有改变检测结论”提供了直接证据。

### 第 5 步：形成全量报告和下一边界

做了什么：新增 `report_skilltrustbench_full.py`，自动生成核心指标、混淆矩阵、T01—T09、失败分布、误报漏报、10% 对照和证据路径。

结果：coverage 97.32%、strict macro F1 0.5090、malicious recall 71.11%、non-normal recall 77.38%、normal FPR 28.67%、策略层 loose F1 81.65%。wild_real_world 仅 38/242（15.70%），T06 为 42/96（43.75%），是最重要短板。

为什么：完整公开数据已经用于最终评测，后续不能继续在同一 5,520 条上反复调参并称为无偏提升；应另建开发集与冻结回归集，再做语义增强和动态验证。

## 当前下一任务（M3）

从 wild_real_world 与 T06/T09 中选择脱敏代表案例建立开发集，同时保留未参与规则设计的冻结回归集；先实现无害动态 fixture 的进程、网络、文件和环境变量事件采集，再评估大模型语义复核是否提高召回且不显著扩大 28.67% 的 normal FPR。

## 2026-08-15：全量基线冻结、开发/回归划分与缺口分析

### 第 1 步：冻结全量比较基线

做了什么：新增 `freeze_skilltrustbench_development.py`，重新调用独立验收逻辑，并将全量结果、指标、混淆矩阵、错误切片、日志、报告、数据清单、运行器、校验器和策略文件的大小及 SHA-256 写入冻结清单。

结果：建立 `skilltrustbench-v1.0-full5520-cisco-static-v1`，`freeze_manifest.json` SHA-256 为 `e4ed096b…17e4`；原始全量运行目录没有修改。

为什么：后续规则开发必须有不可漂移的 Cisco 对照，不能在改代码后用新的结果覆盖旧基线。

### 第 2 步：建立 120 条开发集

做了什么：从 completed 样本中确定性选择 60 条风险筛查漏报、40 条正常误报和 20 条正确对照。漏报重点覆盖 wild_real_world、T06、T09 和 T05；误报覆盖网络、文件、命令、文件类型、密钥和元数据规则。

结果：120 条唯一 ID，标签为 normal 50、suspicious 22、malicious 48；ID 清单 SHA-256 为 `1f9aad62…e383`。

为什么：开发集要集中最有价值的错误，才能在每天 2—3 小时的限制下快速验证规则，而不是重新浏览 5,520 条。

### 第 3 步：建立 600 条封存回归集

做了什么：排除开发集后，按固定 seed 与 case ID 哈希排序，从 normal、suspicious、malicious 各取 200 条。选择过程不读取父扫描预测，输出只保存 ID、标签、case tree hash 和排序值。

结果：600 条唯一 ID，与开发集零重叠；ID 清单 SHA-256 为 `cd83b4f4…c07d`。本轮没有打开任何回归样本正文。

为什么：回归集用于发现规则退化；如果边开发边查看其中的具体错误，就会再次把测试集变成开发集。

### 第 4 步：只读分析开发样本

做了什么：新增 `analyze_skilltrustbench_development.py`，只读取 `SKILL.md` 与脚本文本，提取持久化、权限、网络、文件、进程、密钥和描述能力等规范化特征；不保存正文或代码片段，并在读取前后复核每个 case tree hash。

结果：120/120 完成，hash 变化为 0；建议路线为 39 条新增静态规则、41 条证据关联、9 条语义复核、8 条规则校准、2 条策略分离、1 条动态验证和 20 条正确对照。

为什么：这一步把“Cisco 漏报很多”转化为具体可开发任务，同时避免因复制恶意正文扩大风险。

### 第 5 步：形成优先级

做了什么：将全量指标、开发样本 pattern 和正常误报声明进行关联。

结果：第一优先级为下载—解码—执行、paste 服务载荷、T06 启动项/计划任务/服务持久化；第二优先级为声明—行为—敏感数据流关联，先处理网络、文件和命令高频误报；9 条样本进入结构化大模型语义复核，模型不得直接自动阻断。

为什么：高置信攻击链提高召回，证据关联控制误报；二者必须同时开发，不能只追求检出数量。

### 第 6 步：验证与文档

做了什么：增加 5 项确定性划分、路由和冻结漂移测试，运行全量测试；重复执行冻结与分析流程并比较关键输出 SHA-256；生成 M3 报告并更新计划、检查表和对接文档。

结果：`78 passed`；6 个关键产物重复运行 SHA-256 全部一致。证据位于 `artifacts/analysis/2026-08-15-skilltrustbench-dev120-regression600-v1/`。

为什么：划分和缺口分析也必须可重复，否则下一轮无法证明性能变化来自规则而不是样本变化。

## 当前下一任务（M3 实现）

先实现下载—解码—执行与 T06 持久化两个规则包，再实现声明—行为—敏感数据流关联；只在开发集和 20 条正确对照上调试。规则、阈值与语义提示词冻结后，再一次性运行 600 条回归集配对评测。

## 2026-08-15：通用政企平台规则补强必要性说明

做了什么：基于已冻结的 5,520 条全量指标和 120 条开发集缺口分析，新增 `docs/M3_通用政企智能体平台规则补强必要性说明.md`，将下载执行、系统持久化、权限边界、敏感数据流、描述—行为一致性、误报治理、元数据策略分离和动态验证逐项映射到通用政企智能体平台风险。

结果：文档明确区分已完成的数据治理/分析工程与尚未接入的正式检测规则，提供候选规则、处置建议、实现优先级、回归验收口径和正式汇报表述。

为什么：老师和平台负责人需要理解“为什么必须补这些规则”，而不仅是看到一组技术名称或扫描指标；同时必须避免把候选方案误报为已实现能力。

## 2026-08-16：Aegis Static v1 第一批规则实现

### 第 1 步：接入独立静态增强层

做了什么：新增 `backend/analyzers/aegis_static.py`，并在 Cisco Skill Scanner 归一化之后、统一策略之前合并 Aegis Finding。Cisco 适配器和原始 Finding 保持不变，健康检查与扫描结果新增 `aegis-static-v1` analyzer 身份。

结果：远程载荷与持久化规则已进入真实 API 扫描编排，不再只是分析文档中的候选方案。

为什么：独立增强层既保留厂商基线可复现性，也允许自研规则独立评估、校准和替换。

### 第 2 步：实现高置信攻击链与证据分级

做了什么：实现远程内容直接进入 shell、下载—解码—执行、临时粘贴站载荷、内嵌编码载荷，以及计划任务、系统服务、启动位置写入规则；跨文件或不完整证据统一降为 `MEDIUM/REVIEW`。

结果：完整链进入 `CRITICAL/BLOCK`，弱组合证据进入人工复核。Finding 使用确定性 ID、相对路径和规范化证据代码，不保存恶意正文。

为什么：单个网络、编码或进程关键词误报率高，政企准入需要证明“多个原子行为组成可执行攻击链”才能自动阻断。

### 第 3 步：三轮开发集校准

做了什么：首轮运行 120 条开发集后，检查两条 normal 严重度升级；把 raw GitHub 与临时粘贴站分离，把普通配置写入与自动执行载荷分离；第二轮继续排除 `source ~/.bashrc` 的过度关联，形成 v3；随后将关联算法由笛卡尔积改为滑动窗口并增加命中上限，形成 v4。

结果：最终目标补出 21/36，T06 12/12，wild real-world 9/24；20 条正确对照零回退，50 条 normal 开发样本零决策升级。v3 与 v4 逐案决策和规则集合差异为 0；四轮结果分别保存，没有覆盖历史证据。

为什么：规则开发不能只追求召回；误报升级必须通过证据语义校准消除，而不是在汇报中忽略。

### 第 4 步：性能、安全与端到端验证

做了什么：对每条开发样本做分析前后 tree hash 校验，记录脱敏逐案结果、指标、日志和运行清单；运行新增 10 项规则测试和完整后端测试；真实扫描安全/风险预置 Skill。

结果：120 条样本 hash 变化为 0，回归集打开数 0；Aegis v4 平均 17.27 ms、最大 178 ms；3,000 组重复特征洪泛测试和完整测试 `89 passed`。安全预置保持 `ALLOW`，风险预置保持 `BLOCK`，两者 analyzer 列表均包含 Aegis。

为什么：这同时证明了规则有效性、输入安全边界、工程性能和 API 实际接入状态。

## 当前下一任务（M3 第二批）

实现声明—行为—敏感数据流关联，对 Cisco 的网络、文件系统和命令类 Finding 增加“是否在 SKILL.md 声明、是否接触敏感来源、是否流向外部/执行 sink”的上下文证据。只在开发集调试；规则冻结后再一次性打开 600 条回归集做配对评测。

## 2026-08-18：Aegis Network Context v1 网络旁路证据

### 第 1 步：冻结 INFO-only 实验契约

做了什么：与用户确认第一版只增加网络上下文，不修改 Cisco Finding、不自动降低或提高风险、不改变最终决策；数据只使用 16 条网络误报和 20 条正确对照。

结果：运行契约明确要求 36/36 决策不变，600 条回归集继续封存。

为什么：声明存在不能自动证明实现安全，先建立可解释证据比直接修改门禁更稳妥。

### 第 2 步：实现声明—行为—敏感流关联

做了什么：新增 `network_context.py`，提取网络/外发/鉴权声明，识别读取型网络行为、外发 sink、环境变量、凭据路径、敏感标识符和认证语法；输出固定 `INFO` Finding，并引用相关 Cisco rule ID。

结果：实现 9 类上下文规则，包括已声明/未声明、只读行为、外发是否声明、SDK/封装、mock/local-only、敏感来源共现和正常鉴权可能性。

为什么：Cisco 网络告警需要“为什么是业务需要、是否真的发送数据、凭据是否只是鉴权”三层解释，而不是用单一关键词直接判定。

### 第 3 步：两轮覆盖校准和一次口径修正

做了什么：v1 覆盖 10/16 后，只检查 6 条无上下文开发样本；增加通用 `session/client` 网络调用、SDK/封装声明和 mock/local-only 证据。v3 只修正已声明总数的汇总口径。

结果：最终覆盖 16/16；15 条明确声明网络能力，1 条未声明；12 条识别直接原语，3 条为 SDK/封装，1 条为 mock/local-only。v2/v3 逐案 Finding 和决策差异为 0。

为什么：不按 case ID 或产品名硬编码，保证补强来自通用机制；统计口径修正必须与检测规则修改分开记录。

### 第 4 步：不变性、性能与端到端验证

做了什么：逐条重建 Cisco + Aegis Static v4 决策，加入上下文后再次执行策略；核验样本前后 tree hash、规则严重度、运行输出哈希和真实预置 Skill。

结果：36/36 决策不变，20/20 正确对照不变，Aegis Static v4 等价 36/36；平均 12.78 ms、最大 35 ms；完整测试 `100 passed`。安全预置保持 `ALLOW`，风险预置保持 `BLOCK`，风险样本只增加 3 条 INFO 解释。

为什么：本轮目标是解释性增强而非策略优化，决策不变本身就是核心验收指标。

## 当前下一任务（M3 第三批）

按相同 INFO-only 方式实现文件系统声明—实际读写—敏感路径上下文，优先覆盖开发集 `fp_filesystem_context`；网络旁路证据继续保持不参与门禁。

## 2026-08-18：Aegis Filesystem Context v1 文件系统旁路证据

### 第 1 步：冻结 INFO-only 与数据边界

做了什么：与用户确认只增加文件系统上下文，不修改 Cisco Finding、不改变门禁；数据只使用 8 条文件系统误报和 20 条正确对照，600 条回归集继续封存。

结果：合同要求覆盖 8/8、决策不变 28/28、对照不变 20/20、Static v4 差异 0、样本哈希变化 0。

为什么：文件能力声明不能直接证明安全，先建立解释证据可以避免过早的自动降级掩盖越权路径或敏感文件访问。

### 第 2 步：实现声明—读写—路径—修改关联

做了什么：新增 `filesystem_context.py`，识别文件能力与读写声明、实际读/写/探测、工作区和临时路径、敏感和系统路径、覆盖、删除、递归修改与路径 containment guard；所有 Finding 固定为 `INFO`。

结果：分析器接入统一扫描链、健康状态和 API；新增 12 项专项测试，覆盖策略不变、复杂度上限和端到端接线。

为什么：政企审核既要解释正常知识库、报告导出和业务状态文件，也要暴露凭据、用户资料、系统路径与破坏性修改风险。

### 第 3 步：保留 v1 偏差并完成 v2 校准

做了什么：v1 运行后发现声明识别 2/8，与冻结人工特征 7/8 不一致，并存在 Markdown 示例被当作实际行为、同类路径 Finding 重复的问题。保留 v1 全部产物，v2 仅扩展通用自然语言词形、把实际行为限定为源文件并按 Skill 聚合路径证据。

结果：v2 达到目标覆盖 8/8、声明/未声明 7/1；工作区/临时路径 7、敏感路径 5、系统路径 2、覆盖能力 7、删除 2、递归修改 1、路径保护 1。没有按 case ID 或产品名硬编码。

为什么：实验不能只看覆盖率；解释分类与人工冻结证据不一致时必须保留失败轮次并校准，不能直接包装为成功。

### 第 4 步：不变性、性能与真实 Cisco 验证

做了什么：逐案重建 Cisco + Static v4 + Network Context 决策，再加入 Filesystem Context；核对样本前后树哈希、输出 SHA-256、完整测试和真实 Cisco 演示样本。

结果：28/28 决策不变，20/20 对照不变，Static v4 等价 28/28，样本哈希不变 28/28，回归读取 0；平均 15.32 ms、最大 53 ms；完整测试 `112 passed`。真实 BLOCK 演示样本仍为 BLOCK，Analyzer 列表正确且样本哈希不变。

为什么：本轮目标是解释性增强，门禁不变、基线不漂移和证据可复核是核心验收条件。

## 2026-08-18：Aegis Command Context v1 命令旁路证据

### 第 1 步：冻结命令解释合同

做了什么：只选择 6 条 `fp_command_context` 和 20 条正确对照；Cisco 全量基线、Static v4、Network、Filesystem 与统一门禁保持只读，600 条回归正文不打开。全部新 Finding 固定为 INFO。

结果：冻结目标覆盖 6/6、五类关键机制 5/5、决策不变 26/26、对照不变 20/20、Static v4 差异 0 和样本哈希差异 0。

为什么：声明、测试夹具和静态近邻不能单独证明安全；先把解释机制与风险裁决分离，避免过早降级 Cisco 告警。

### 第 2 步：实现声明—调用—输入—危险类别关联

做了什么：新增 `command_context.py`，区分仅导入、argv/非 shell、shell 字符串、shell 脚本、固定/动态可执行文件、stdin、用户/环境/文件来源、安全测试夹具、只读命令、校验线索和五类危险命令；接入统一 Finding、扫描链与健康状态。

结果：新增 14 项专项测试，所有规则固定 INFO；最多 500 文件、单文件 1 MB、累计 5 MB、2,048 特征和 80 行关联窗口。

为什么：政企平台既需要正常调用办公、数据库、媒体和运维工具，也必须区分 shell 注入、动态程序选择、提权、持久化和安装副作用。

### 第 3 步：保留 v1 并完成 v2 解释校准

做了什么：v1 指标通过后逐案人工复核，发现只导入 `child_process` 的 `case_00458` 中普通 JavaScript 模板字符串被误解为 shell 引号变量。保留 v1 全部产物，v2 只要求该规则与同文件 shell 字符串调用或 shell 脚本共现。

结果：quoted-shell 目标计数从 2 收紧为 1；6/6 覆盖和 5/5 机制保持，未按 case ID 或产品名硬编码。

为什么：INFO 也必须准确；不影响门禁的误导性解释仍会降低评委对证据链的信任。

### 第 4 步：不变性、性能与真实 Cisco 验证

做了什么：逐案重建 Cisco + Static v4 + Network + Filesystem 决策，再加入 Command Context；核对样本哈希、完整测试、证据清单和真实 Cisco 演示。

结果：最终 v2 覆盖 6/6、机制 5/5、决策不变 26/26、对照不变 20/20、Static v4 差异 0、样本哈希差异 0、回归读取 0；平均 20.58 ms、最大 91 ms；专项测试 14 passed，完整测试 `126 passed`。真实 Cisco 演示约 4.4 秒，仍为 BLOCK，Command Context 对无命令行为样本返回 0 条，样本哈希不变。

为什么：上下文层的验收重点是可解释、无副作用和可复核，而不是用 INFO 数量制造表面效果。

## 当前下一任务（M3 第五批）

建立最小安全动态 fixture：不执行 SkillTrustBench 数据集样本，只运行自建良性/可控程序，观测子进程、参数、stdin、环境变量、文件和网络副作用，并实现超时与失败闭锁。最终决策继续不变，600 条回归集继续封存。

## 2026-08-18：最小安全动态 Fixture v1

### 第 1 步：冻结安全执行边界

做了什么：与用户确认第一版为独立 CLI、Windows + Python，只运行自建良性 fixture，网络仅 `127.0.0.1`，不执行任何数据集或第三方样本，证据 INFO-only、决策不变。

结果：冻结 3/3 fixture、7/7 机制、违规/超时/泄露/样本接触/互联网/决策变化均为 0 的验收合同。

为什么：没有 Docker、VM 或云沙箱时，直接执行第三方 Skill 不具备可接受的隔离与回滚条件。

### 第 2 步：实现哈希锁定与协作式观测

做了什么：新增 dynamic policy、audit bootstrap、runner、独立 CLI 和三份 fixture；只记录进程、stdin、环境、文件、回环网络的长度、相对路径与 SHA-256，不保留原始输入和输出。

结果：fixture 配置锁定脚本哈希、唯一 Python 子命令、5 秒超时和预期事件；工作区写入、chdir、链接、回环地址/端口与进程均 fail-closed。

为什么：动态证据首先必须可复核、可脱敏且不会把任意脚本带入执行面。

### 第 3 步：两轮 smoke 与 v1 封口审查

做了什么：两轮 smoke 确认 Windows audit 使用 `(None, exact command-line string, ...)`；改为父 Runner 生成唯一命令行 SHA-256。v1 主运行通过后又发现 cwd 相对路径和链接边界需显式收紧，因此保留 v1，建立 v2。

结果：所有失败均在策略闭锁路径发生，没有未批准子进程或外部动作；v2 增加实际 cwd 解析、workspace 内 chdir、拒绝软/硬链接和准确的 Windows 证据字段。

为什么：动态安全边界不能只依赖当前 fixture“恰好没有触发”，必须让代码和测试明确表达拒绝条件。

### 第 4 步：v2 主运行与完整验证

做了什么：使用相同三份 fixture 和 7 项机制重新运行，随后执行完整测试、token/样本路径扫描和证据哈希复核。

结果：3/3、7/7；进程/stdin/环境为 1/1/3，文件读写 1/1，回环连接/服务端接收 1/1；所有负面指标为 0；专项测试 10 passed，完整测试 `136 passed`。

为什么：本轮只证明可信 fixture 的动态证据契约，不声称已能安全执行恶意样本。

## 当前下一任务（M3 第六批）

把固定 `fixture_set_id` 的内置动态审计接入管理员专用 API 和结果页面，不接受任意路径或上传代码，继续保持 INFO-only、决策不变和第三方样本执行 0。

## 2026-08-18：管理员动态 Fixture API 与页面

### 第 1 步：冻结鉴权与输入边界

做了什么：与用户确认服务端只从 `AEGIS_ADMIN_TOKEN` 读取令牌，请求头固定为 `X-Aegis-Admin-Token`；前端令牌仅放 React 内存；接口只运行固定内置 fixture，不接收路径、上传、脚本、命令或自定义配置。

结果：未配置服务端令牌时 503，令牌缺失/错误时 401；创建接口含任何请求体时 400。动态证据继续 INFO-only、policy effect none、决策变化 0。

为什么：没有 Docker/VM 时不能把平台接口变成任意代码执行入口；本阶段定位是机制自检，不是第三方 Skill 沙箱。

### 第 2 步：实现异步任务、历史和页面

做了什么：新增 `DynamicAuditJob`、独立 SQLite 表、后台 worker、创建/列表/详情 API；前端新增管理员页面、内存令牌、自动轮询、逐 fixture 结果、INFO 事件和历史。

结果：任务工作区执行后删除；SQLite 只保存脱敏结果；响应不含 fixture 绝对路径和令牌。公开静态扫描接口保持原样。

为什么：独立任务模型避免把“机制完成”误解为 ALLOW，也避免污染静态扫描的四态准入语义。

### 第 3 步：修复 Windows 资源清理与验证假阳性

做了什么：独立验证器前两轮在退出时发现 SQLite 句柄未可靠释放，后端所有数据库访问改为显式关闭；随后把将说明文字误判成浏览器存储调用的检查收紧为真实 API 语法。

结果：临时数据库和任务工作区均可删除；失败轮次保留在本阶段 artifact 目录，没有覆盖。

为什么：Windows 上“事务提交”不等于“连接关闭”；动态安全功能必须把资源回收也纳入验收。

### 第 4 步：真实 API 与完整回归

做了什么：通过受保护 API 真实执行固定 fixture 集，检查 16 项 API/安全条件；再跑后端、前端和生产构建。

结果：创建/详情/历史为 202/200/200，自定义请求体 400；3/3、7/7，所有负面指标为 0；后端 `142 passed`、前端 `9 passed`、生产构建通过。

为什么：接口可用、令牌不持久化、工作区清理和静态链路不回退必须同时成立，不能只展示一张动态事件表。

## 当前下一任务（M3 第七批）

在不扩大执行对象的前提下，为管理员动态任务增加服务端并发限制、非敏感操作审计和统一平台对接字段；第三方样本动态执行继续等待 Docker/VM/远程沙箱等隔离基础设施。
