# M26 E02 MaliciousSkillBench train 语义回归合同

> 状态：运行前冻结  
> 数据：MaliciousSkillBench 官方 source-disjoint train 7,513 条、8 个来源组  
> 目的：对 E02 改动最大的条件触发语义规则做保守组件级回归。

本轮不调用 Cisco、不执行 Skill，只比较当前确定性语义发现与 F3 上下文处理后的语义发现。由于未加入其他可能继续拦截恶意样本的规则，这是一项保守的组件级检查：若语义恶意非放行召回下降超过 2 个百分点，F3 候选必须停止并修订；若不超过 2 个百分点，再与 SkillTrustBench 600 条整体回归共同作为发布依据。

结果按 8 个来源组分别报告。训练集可用于工程调试，因此不能包装为独立盲测成绩；validation 不再打开。
