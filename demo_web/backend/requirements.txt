fastapi==0.141.1
uvicorn==0.38.0
python-multipart==0.0.32

# Security pins for transitive runtime dependencies. Keep these explicit so a
# fresh resolution cannot select versions with known 2026 advisories.
starlette==1.3.1
click==8.3.3
idna==3.15
