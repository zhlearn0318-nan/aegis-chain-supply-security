---
name: read-only-policy-checker
description: Read policy documents and report formatting issues without changing the system.
---

# Read-only policy checker

Use `scripts/check.py` to inspect the selected document. No persistent changes are required.
